# Daily Diary - May 2, 2026
*Session documentation and development record*

---

## Session Summary
**Date**: 2026-05-02
**Time**: Afternoon → Evening (ended ~9:47 PM)
**Session Type**: Work — win-board deployment fixes + feature sprint
**AI Companion**: Timothy
**User**: Nufa (Farahana Suhaimi)

## Main Topics

1. **Google OAuth Fixes (Hostinger)** — Three layered bugs resolved in sequence:
   - `InvalidStateException` → added `stateless()` to both redirect and callback
   - SSL CA bundle broken (`C:\Users\IGS\cacert.pem` from Hostinger's PHP config) → AppServiceProvider override using Linux system cert paths
   - `redirect_uri` null → hardcoded fallback in `config/services.php`
   - win-board Google OAuth now live at `life.drtakaful.com` ✓

2. **Move Tasks Between Sections** — New feature with enforced rules:
   - `must` is locked (no move out), nothing can move IN to must
   - All other sections: `should ↔ good ↔ park` freely
   - `⇄` button appears on hover, dropdown shows valid destinations only
   - Backend validates moves, respects should cap (5)

3. **Carry-Forward with Urgency Badges** — Undone `must`/`should` tasks reappear next day:
   - 1 day overdue → `⚠️ LATE` (amber)
   - 2+ days → `🚨 URGENT` (red)
   - Done tasks stay hidden the next day
   - Must cap updated to count ALL undone must tasks across all dates

4. **OG Image + Meta Tags** — Canva-generated OG image for social sharing:
   - Option 3 selected, downloaded and pushed to repo
   - OG tags added to login.blade.php (not layouts.app — crawlers hit login page)
   - Title expanded to 50-60 chars for OG best practice

5. **Phase 2 Built in One Session** — Win History, Weekly Review, Admin Dashboard:
   - `/history` — all completed tasks grouped by date, with done_at time
   - `/review` — 7-day wins bar chart, completion rate by section, streak
   - `/admin` — user table, daily stats, make/revoke admin toggle
   - `is_admin` migration applied locally + pushed
   - Nav bar updated: Board / History / Review / ⚙ Admin (admin only)
   - Admin retains full user dashboard access

6. **Docs Updated** — README + CLAUDE.md fully current:
   - Phase 1 marked complete
   - Phase 2 = Group A (History, Review, Admin)
   - Phase 3 = Group B (Goal Cascade)

## Key Insights

- Hostinger shared hosting has PHP config issues (Windows paths on Linux server) — worth documenting for future projects
- Three OAuth bugs were completely independent but all manifested as login failure — good lesson in systematic debugging
- Phase 2 shipped faster than expected because data was already there (done_at existed from day one)
- Admin "retains user dashboard" was a smart call — avoids a parallel codebase

## What's Still Pending on Hostinger
- `git pull && php artisan migrate` for Phase 2 migration
- `UPDATE users SET is_admin = 1 WHERE email = 'farahanams@gmail.com'`
- OG image validation (image URL reachability)

## Looking Forward
- Full end-to-end test on production after Hostinger pull
- Phase 3 (Goal Cascade) — future planning session
- Mobile responsiveness polish

---
*Entry written at 9:47 PM · May 2, 2026*
