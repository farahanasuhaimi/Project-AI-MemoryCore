# Daily Diary - April 29, 2026
*Session documentation and development record*

---

## Session — 15:56 | New Project: Daily Win Board

**Theme**: New Laravel 12 project scaffolded from scratch — win-board

### What We Built
Started and completed the full scaffold for **Daily Win Board** (`D:\Kerja\Codes\win-board`) — a commitment-first daily task board built for Nufa's own productivity system.

**Stack**:
- Laravel 12.58 + Tailwind v4 via `@tailwindcss/vite`
- MySQL (`daily_win_board` DB)
- Google OAuth via Laravel Socialite
- Gumroad-style UI (Syne + DM Sans + JetBrains Mono, thick black borders, hard shadows)

**What got done in one session**:
- Full migrations: `users` (+ google fields), `daily_commits`, `tasks`, `user_stats`
- Models with casts, fillable, soft deletes
- Controllers: `AuthController`, `DashboardController`, `TaskController`, `CommitController`
- 15 routes wired and verified
- Dashboard view: commitment lock, 4-tier task board (Must/Should/Good/Parking Lot), streak + wins bar, celebration toasts, reset modal — all AJAX
- Google OAuth login working after fixing Windows SSL cert issue
- Precompiled Vite assets pushed to GitHub for Hostinger deploy

### Key Decisions
- Laravel 12 over 11 (Nufa's preference)
- Tailwind v4 `@theme` CSS-first config (no `tailwind.config.js`)
- SSL fix done at app level (Guzzle `verify` in `AppServiceProvider`) — no php.ini admin needed

### Issues Hit & Fixed
1. **Scaffold in non-empty dir** — created temp dir, moved files manually
2. **`InvalidStateException`** — server restarted mid-OAuth flow, resolved on retry
3. **cURL SSL cert error on Windows** — downloaded `cacert.pem` to `C:\Users\IGS\cacert.pem`, configured Guzzle in `AppServiceProvider`

### Pushed to GitHub
`https://github.com/farahanasuhaimi/win-board` — 3 commits including precompiled build assets

### Next Steps
- Deploy to Hostinger (SSH → git clone → composer install → migrate)
- Update Google OAuth callback URL in Google Cloud Console for production domain
- End-to-end dashboard test (task CRUD, streak, toasts, reset)

---
