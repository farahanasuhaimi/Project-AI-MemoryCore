# Daily Diary - May 17, 2026
*Session documentation and development record*

---

## Session Summary
**Date**: 2026-05-17
**Time**: Morning → 12:32
**Machine**: Home PC (GPU machine)
**Session Type**: Infrastructure + Exploration
**AI Companion**: Timothy

## 🎯 Main Topics

### 1. cms-takaful — ReachAngleSeeder
Ran the pending SSH seeder from last session (office had timed out). Confirmed: 10 angles seeded for `admin@drtakaful.com`. Clean tick off the pending list.

### 2. Hermes Agent + Local LLMs — Exploration
Nufa asked about using Hermes for rox-bot — starting from curiosity about local LLM agents in general. We designed and built a full Hermes+Gemma4 agent architecture for rox-bot.

**Architecture built:**
- `rox_agent.py` — new agent loop
  - **Gemma4** (already on Ollama): vision layer — reads screenshot, classifies game state
  - **Hermes3** (pulled 4.7GB during session): agent brain — tool calling to decide action
  - **ADB** (LDPlayer `emulator-5556`, 1600×900): replaces pyautogui entirely — taps without window focus

**ADB integration:**
- LDPlayer found at `C:\LDPlayer\LDPlayer9\adb.exe`
- Connected to `emulator-5556`
- Screenshot via `exec-out screencap -p` → numpy array → vision model
- Tap via `adb shell input tap x y`

**Key improvements to rox_gardening.py:**
- Multi-scale template matching for gardening button (handles resolution mismatch)
- `detect_numpad_buttons()` — color-based numpad grid detection, with grid validation + bottom-half constraint to eliminate false positives
- `find_captcha_input_bar()` — dark rounded input bar detection by color
- `find_captcha_confirm_button()` — blue confirm button detection by color
- All template paths made absolute (`os.path.dirname(__file__)`)

**CAPTCHA solve flow (correct sequence confirmed by Nufa):**
1. Click dark input bar below expression → numpad slides up
2. Detect numpad buttons (fresh screenshot, validated 3×4 grid)
3. Click digit buttons
4. Click Confirm

**What worked:**
- Agent loop running (Gemma4 → Hermes3 → ADB)
- Hermes3 bypassed for CAPTCHA (deterministic, no need for LLM decision)
- Gemma4 successfully locates input bar coordinates
- Numpad grid detection with validation

**What's still pending:**
- Gemma4's input bar coordinates slightly off — debug image `debug_captcha.png` auto-saves for visual verification next session
- OCR expression reading region needs the correct input bar position to crop properly
- Once coordinates confirmed, full solve flow should work end-to-end

## 💡 Key Insights

- **ADB > pyautogui** for emulator automation — works in background, no window focus, clean game-only screenshots
- Gemma4 is good at high-level state detection but imprecise for exact pixel coordinates — hybrid approach (Gemma4 for state + OCR for expression) is more robust than all-vision
- Hermes should only be in the loop for non-trivial decisions — CAPTCHA solving is deterministic and should bypass it

## 🔮 Next Steps (rox-bot)
- Run bot with CAPTCHA visible → check `debug_captcha.png` → verify Gemma4's input bar coordinate
- If coordinates consistently off, apply a fixed offset or recalibrate prompt
- Once input bar click lands correctly, full loop should complete

---
*Entry written at 12:32 — home PC session*
