# Daily Diary - May 6, 2026
*Session documentation and development record*

---

## Session Summary
**Date**: 2026-05-06
**Time**: ~17:33 – 22:57
**Session Type**: Work — win-board bug fixes
**Project**: win-board (`D:\WebDev\win-board`)

## Main Topics

1. **Parking lot carry-forward**: Nufa reported park tasks not appearing the next day. Code already had 'park' in the carry-forward query, but the fix wasn't explicit enough. Added a proactive `update(['date' => $today])` step (mirroring the should→must escalation pattern) so park tasks always roll to today on dashboard load. Cleaner and more reliable.

2. **Non-negotiable reverted to plain input**: Commit `50c1955` had introduced a "Pick from your Must tasks" picker above the text input. Nufa wanted back to the original — just a plain text field and lock button. Removed the picker block, `selectedCommitTaskId` JS variable, and `selectCommitTask()` function entirely.

3. **LATE/URGENT badge emoji alignment**: The 🚨/⚠️ emoji inside small `text-[10px]` badges was causing vertical misalignment because emoji characters inflate the line-box. First attempt used `inline-flex items-center leading-none` — didn't fully fix it. Second attempt removed the emoji (Nufa wanted them back). Final fix: `h-5` fixed height + `overflow-hidden` + `inline-flex items-center` so the emoji can't expand the badge beyond its box.

## Commits Pushed
- `c073ec9` — parking lot carry-forward + non-negotiable plain input + first badge attempt
- `c36a5f9` — removed emoji from badges (wrong direction)
- `f6f7948` — final badge fix with fixed height + overflow-hidden

## Collaboration Highlights
- Nufa pushed back on removing the emoji — good instinct, the emoji adds clarity
- Three iterations on the badge fix; got there in the end
- Quick, direct session — no scope creep, focused on what was broken

## Looking Forward
- **win-board**: Deploy to Hostinger (PWA + all fixes now ready)
- **drtakaful**: Resume FAQPage schema from `kenapa-takaful-penting.html`
- **rox-bot**: Run `test_ocr.py`
- **cms-takaful**: Awaiting deploy

---
*Entry written: 22:57*
