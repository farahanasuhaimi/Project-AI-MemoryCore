# Daily Diary - April 30, 2026
*Session documentation and development record*

---

## Session Summary
**Date**: 2026-04-30
**Time**: ~17:51 – 23:09 MPST
**Session Type**: Work — win-board environment setup
**Focus**: Cloning and configuring win-board on a new PC (D:\WebDev)

---

## Main Topics

1. **win-board not on this PC** — Nufa is on a different machine from yesterday. The project exists on another PC at `D:\Kerja\Codes\win-board` but not here. Goal: get it running at `D:\WebDev\win-board`.

2. **Full environment setup from scratch**:
   - Cloned from GitHub: `https://github.com/farahanasuhaimi/win-board`
   - `composer install` completed
   - `.env` configured: MySQL (`daily_win_board`, root/1234qwer), APP_URL, Google OAuth credentials
   - `server.php` created in project root (missing from Laravel 12.58 — known issue)
   - DB created and all 7 migrations ran successfully

3. **Google OAuth SSL issue** — The main blocker of the session. Three errors surfaced in sequence:
   - `InvalidStateException` — fixed by correcting `APP_URL` to `http://localhost:8000`
   - `cURL error 60` — SSL cert problem. Nufa's PC already has `curl.cainfo` set in `php.ini` at `C:\Program Files\php\php.ini`, pointing to `C:\Program Files\php\extras\ssl\cacert.pem`
   - The previous PC's `AppServiceProvider` Socialite SSL workaround was cleaned out since this PC has proper PHP curl config
   - Final state: `services.php` reverted to clean (no guzzle key), `AppServiceProvider` cleaned up — waiting to test at next session

---

## Key Facts Discovered

- This PC's username: `User` (vs `IGS` on the other PC)
- MySQL root password on this PC: `1234qwer`
- PHP ini: `C:\Program Files\php\php.ini` — curl.cainfo already configured
- `cacert.pem` exists at `D:\WebDev\cacert.pem` (230KB, 148 certs, Nov 2025) — but PHP's own cert is at `C:\Program Files\php\extras\ssl\cacert.pem`
- `server.php` fix needed for Laravel 12.58 — this is a known framework gap

---

## Next Steps (Tomorrow)

- Restart `php artisan serve` and test Google login with cleaned-up AppServiceProvider
- If SSL still fails: check if `C:\Program Files\php\extras\ssl\cacert.pem` is valid
- If login works: full end-to-end dashboard test
- Then: Hostinger deployment

---

## Collaboration Notes

- Nufa was sharing callback URLs in chat — she was copying from the browser address bar while debugging. Clarified the OAuth flow (browser handles it automatically).
- Good debugging session — methodical elimination of issues (state → SSL cert → cert path → cert source)

---
